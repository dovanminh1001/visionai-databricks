# 📋 VisionAI — Workflow Kế Hoạch Demo (SQL Serverless Edition)
## Databricks Free Trial + Serverless SQL + GitHub CI/CD | Deadline: Thứ 7, 25/04/2026

> **Nguyên tắc:** Chỉ làm những thứ có thể **DEMO LIVE** khi báo cáo.  
> **Thay đổi:** Toàn bộ pipeline sẽ dùng **SQL thuần** chạy trên **Serverless SQL Warehouse** (Free Trial).  
> Không cần tạo Cluster, không cần Python, không cần GPU.

---

## ⚠️ TÌNH TRẠNG HIỆN TẠI (23/04/2026)

| Ngày | Nội dung | Trạng thái |
|---|---|:---:|
| Ngày 1 (19/4) | GitHub repo + cấu trúc | ✅ Đã xong |
| Ngày 2 (20/4) | GitHub Actions CI | ✅ Đã xong |
| **Ngày 3+4 (23/4)** | **Medallion + Dashboard** | **⬅️ LÀM HÔM NAY** |
| Ngày 5 (24/4) | Chuẩn bị demo | Ngày mai |
| Ngày 6 (25/4) | BÁO CÁO | Thứ 7 |

> 💡 Do chuyển sang SQL Serverless, khối lượng công việc giảm đáng kể.  
> Ngày 3 + Ngày 4 có thể **gộp lại làm trong 1 buổi chiều hôm nay**.

---

## 📅 NGÀY 1 — Tối Chủ Nhật 19/4 (~2-3 tiếng) ✅

**🎯 Mục tiêu: Dựng GitHub repo đúng chuẩn**

- [x] Tạo GitHub repo: `visionai-databricks`
- [x] Tạo cấu trúc thư mục
- [x] Push lên GitHub

---

## 📅 NGÀY 2 — Thứ Hai 20/4 ⭐ ✅

**🎯 Mục tiêu: GitHub Actions CI chạy được**

- [x] Viết `ci.yml` trigger khi push/PR
- [x] Push commit → GitHub Actions chạy xanh lá ✅

---

## 📅 NGÀY 3+4 — Thứ Tư 23/4 (HÔM NAY) 🔥

**🎯 Mục tiêu: Toàn bộ Medallion Architecture + Dashboard bằng SQL**

### Bước 1: Khởi động SQL Warehouse

- [ ] Đăng nhập Databricks Workspace (bản Trial hiện tại)
- [ ] Vào **SQL Warehouses** → Kiểm tra có Warehouse nào đang chạy (Status: Running)
- [ ] Nếu chưa có, bấm **Create SQL Warehouse** → Đặt tên `visionai-warehouse` → Size: `2X-Small` → **Create**
- [ ] Đợi đến khi Status hiện **Running** (khoảng 1-2 phút)

### Bước 2: Tạo Catalog & Schema

- [ ] Vào **SQL Editor** (menu bên trái, biểu tượng bút chì)
- [ ] Chạy lần lượt từng lệnh SQL sau:

```sql
-- Tạo Catalog (nếu có quyền, nếu không thì dùng catalog mặc định)
-- CREATE CATALOG IF NOT EXISTS visionai_catalog;
-- USE CATALOG visionai_catalog;

-- Tạo 3 Schema cho kiến trúc Medallion
CREATE SCHEMA IF NOT EXISTS bronze;
CREATE SCHEMA IF NOT EXISTS silver;
CREATE SCHEMA IF NOT EXISTS gold;
```

> 💡 Nếu lệnh `CREATE CATALOG` báo lỗi không có quyền, bỏ qua 2 dòng đó.  
> Dùng luôn catalog mặc định, chỉ cần tạo 3 schema `bronze`, `silver`, `gold` là đủ.

### Bước 3: Tạo bảng Bronze (Dữ liệu thô từ camera)

```sql
-- BRONZE: Dữ liệu thô ghi nhận từ camera AI
-- Đây là tầng đầu tiên trong kiến trúc Medallion
-- Dữ liệu được giữ nguyên gốc, chưa qua xử lý

CREATE OR REPLACE TABLE bronze.camera_feeds (
    feed_id         INT,
    camera_id       STRING,
    image_path      STRING,
    capture_time    TIMESTAMP,
    location        STRING,
    file_size_kb    INT,
    resolution      STRING,
    ingested_at     TIMESTAMP DEFAULT current_timestamp()
);

-- Chèn dữ liệu mẫu (giả lập 15 bản ghi từ 3 camera trong 1 ngày)
INSERT INTO bronze.camera_feeds (feed_id, camera_id, image_path, capture_time, location, file_size_kb, resolution)
VALUES
    (1,  'CAM_01', '/feeds/gate_a/img_0801.jpg', '2026-04-21 08:01:00', 'Gate A - Main Entrance',    2048, '1920x1080'),
    (2,  'CAM_01', '/feeds/gate_a/img_0815.jpg', '2026-04-21 08:15:00', 'Gate A - Main Entrance',    2100, '1920x1080'),
    (3,  'CAM_01', '/feeds/gate_a/img_0830.jpg', '2026-04-21 08:30:00', 'Gate A - Main Entrance',    1980, '1920x1080'),
    (4,  'CAM_02', '/feeds/gate_b/img_0805.jpg', '2026-04-21 08:05:00', 'Gate B - Side Entrance',    1850, '1280x720'),
    (5,  'CAM_02', '/feeds/gate_b/img_0820.jpg', '2026-04-21 08:20:00', 'Gate B - Side Entrance',    1900, '1280x720'),
    (6,  'CAM_02', '/feeds/gate_b/img_0900.jpg', '2026-04-21 09:00:00', 'Gate B - Side Entrance',    1750, '1280x720'),
    (7,  'CAM_03', '/feeds/parking/img_0810.jpg', '2026-04-21 08:10:00', 'Parking Lot B2',           3200, '3840x2160'),
    (8,  'CAM_03', '/feeds/parking/img_0825.jpg', '2026-04-21 08:25:00', 'Parking Lot B2',           3100, '3840x2160'),
    (9,  'CAM_03', '/feeds/parking/img_0910.jpg', '2026-04-21 09:10:00', 'Parking Lot B2',           3050, '3840x2160'),
    (10, 'CAM_01', '/feeds/gate_a/img_1000.jpg', '2026-04-21 10:00:00', 'Gate A - Main Entrance',    2200, '1920x1080'),
    (11, 'CAM_01', '/feeds/gate_a/img_1400.jpg', '2026-04-21 14:00:00', 'Gate A - Main Entrance',    2150, '1920x1080'),
    (12, 'CAM_02', '/feeds/gate_b/img_1100.jpg', '2026-04-21 11:00:00', 'Gate B - Side Entrance',    1820, '1280x720'),
    (13, 'CAM_02', '/feeds/gate_b/img_1500.jpg', '2026-04-21 15:00:00', 'Gate B - Side Entrance',    1880, '1280x720'),
    (14, 'CAM_03', '/feeds/parking/img_1200.jpg', '2026-04-21 12:00:00', 'Parking Lot B2',           3300, '3840x2160'),
    (15, 'CAM_03', '/feeds/parking/img_1800.jpg', '2026-04-21 18:00:00', 'Parking Lot B2',           2900, '3840x2160');

-- Kiểm tra kết quả
SELECT * FROM bronze.camera_feeds ORDER BY capture_time;
```

### Bước 4: Tạo bảng Silver (Dữ liệu đã xử lý & làm giàu)

```sql
-- SILVER: Dữ liệu đã được làm sạch, chuẩn hóa và thêm thông tin
-- Thêm các cột phân tích: giờ trong ngày, loại vị trí, chất lượng ảnh
-- Đây là tầng phục vụ cho Data Scientist và AI Engineer

CREATE OR REPLACE TABLE silver.camera_processed AS
SELECT
    feed_id,
    camera_id,
    image_path,
    capture_time,
    location,
    file_size_kb,
    resolution,

    -- Feature Engineering: Trích xuất giờ trong ngày
    HOUR(capture_time)  AS hour_of_day,

    -- Feature Engineering: Phân ca làm việc
    CASE
        WHEN HOUR(capture_time) BETWEEN 6 AND 11  THEN 'MORNING'
        WHEN HOUR(capture_time) BETWEEN 12 AND 17 THEN 'AFTERNOON'
        WHEN HOUR(capture_time) BETWEEN 18 AND 22 THEN 'EVENING'
        ELSE 'NIGHT'
    END AS time_shift,

    -- Feature Engineering: Phân loại vị trí
    CASE
        WHEN location LIKE '%Gate%'    THEN 'entrance'
        WHEN location LIKE '%Parking%' THEN 'parking'
        WHEN location LIKE '%Lobby%'   THEN 'lobby'
        ELSE 'other'
    END AS location_type,

    -- Feature Engineering: Phân loại chất lượng ảnh dựa trên dung lượng
    CASE
        WHEN file_size_kb >= 3000 THEN 'HIGH'
        WHEN file_size_kb >= 2000 THEN 'MEDIUM'
        ELSE 'LOW'
    END AS quality_bucket,

    -- Audit: Thời gian xử lý
    current_timestamp() AS processed_at

FROM bronze.camera_feeds;

-- Kiểm tra kết quả Silver
SELECT * FROM silver.camera_processed ORDER BY capture_time;
```

### Bước 5: Tạo bảng Gold (Dữ liệu tổng hợp cho báo cáo)

```sql
-- GOLD: Dữ liệu tổng hợp sẵn sàng cho Dashboard và báo cáo kinh doanh
-- Đây là tầng cuối cùng, phục vụ cho nhà quản lý xem KPI

-- Bảng Gold 1: Thống kê theo Camera
CREATE OR REPLACE TABLE gold.detection_by_camera AS
SELECT
    camera_id,
    location,
    location_type,
    COUNT(*)                    AS total_captures,
    ROUND(AVG(file_size_kb), 0) AS avg_file_size_kb,
    MIN(capture_time)           AS first_capture,
    MAX(capture_time)           AS last_capture,
    SUM(CASE WHEN quality_bucket = 'HIGH' THEN 1 ELSE 0 END)   AS high_quality_count,
    SUM(CASE WHEN quality_bucket = 'MEDIUM' THEN 1 ELSE 0 END) AS medium_quality_count,
    SUM(CASE WHEN quality_bucket = 'LOW' THEN 1 ELSE 0 END)    AS low_quality_count
FROM silver.camera_processed
GROUP BY camera_id, location, location_type;

-- Bảng Gold 2: Thống kê theo Ca làm việc
CREATE OR REPLACE TABLE gold.detection_by_shift AS
SELECT
    time_shift,
    COUNT(*)                    AS total_captures,
    COUNT(DISTINCT camera_id)   AS active_cameras,
    ROUND(AVG(file_size_kb), 0) AS avg_file_size_kb
FROM silver.camera_processed
GROUP BY time_shift
ORDER BY
    CASE time_shift
        WHEN 'MORNING'   THEN 1
        WHEN 'AFTERNOON' THEN 2
        WHEN 'EVENING'   THEN 3
        WHEN 'NIGHT'     THEN 4
    END;

-- Bảng Gold 3: Bảng tổng hợp theo giờ (cho biểu đồ)
CREATE OR REPLACE TABLE gold.hourly_activity AS
SELECT
    hour_of_day,
    COUNT(*)                  AS total_captures,
    COUNT(DISTINCT camera_id) AS active_cameras
FROM silver.camera_processed
GROUP BY hour_of_day
ORDER BY hour_of_day;

-- Kiểm tra kết quả Gold
SELECT '--- BY CAMERA ---' AS report;
SELECT * FROM gold.detection_by_camera;

SELECT '--- BY SHIFT ---' AS report;
SELECT * FROM gold.detection_by_shift;

SELECT '--- BY HOUR ---' AS report;
SELECT * FROM gold.hourly_activity;
```

### Bước 6: Tạo bảng giả lập MLflow Tracking (Thay thế MLflow UI)

```sql
-- GIẢ LẬP MLFLOW: Bảng log thí nghiệm huấn luyện mô hình AI
-- Trong thực tế sẽ dùng MLflow Python SDK, ở đây ta dùng SQL để mô phỏng

CREATE OR REPLACE TABLE gold.ml_experiment_log (
    run_id          STRING,
    experiment_name STRING,
    model_name      STRING,
    model_version   STRING,
    -- Hyperparameters
    learning_rate   DOUBLE,
    batch_size      INT,
    epochs          INT,
    img_size        INT,
    -- Metrics
    precision_score DOUBLE,
    recall_score    DOUBLE,
    map50           DOUBLE,
    map50_95        DOUBLE,
    -- Metadata
    dataset_size    INT,
    training_time   STRING,
    status          STRING,
    created_at      TIMESTAMP DEFAULT current_timestamp()
);

INSERT INTO gold.ml_experiment_log
    (run_id, experiment_name, model_name, model_version, learning_rate, batch_size, epochs, img_size, precision_score, recall_score, map50, map50_95, dataset_size, training_time, status)
VALUES
    ('run_001', '/VisionAI/yolov8', 'yolov8n', 'v1.0', 0.01,  16, 50,  640, 0.72, 0.68, 0.71, 0.45, 500,  '12m 30s', 'COMPLETED'),
    ('run_002', '/VisionAI/yolov8', 'yolov8n', 'v1.1', 0.005, 16, 100, 640, 0.78, 0.74, 0.76, 0.52, 500,  '24m 15s', 'COMPLETED'),
    ('run_003', '/VisionAI/yolov8', 'yolov8s', 'v2.0', 0.005, 32, 100, 640, 0.85, 0.81, 0.83, 0.61, 1000, '45m 00s', 'COMPLETED'),
    ('run_004', '/VisionAI/yolov8', 'yolov8s', 'v2.1', 0.001, 32, 150, 640, 0.89, 0.86, 0.88, 0.67, 1000, '1h 05m',  'COMPLETED'),
    ('run_005', '/VisionAI/yolov8', 'yolov8m', 'v3.0', 0.001, 16, 200, 640, 0.92, 0.89, 0.91, 0.72, 2000, '2h 30m',  'COMPLETED');

-- Xem kết quả training
SELECT
    run_id,
    model_name,
    model_version,
    CONCAT('lr=', learning_rate, ' bs=', batch_size, ' ep=', epochs) AS hyperparams,
    ROUND(precision_score * 100, 1) AS `Precision (%)`,
    ROUND(recall_score * 100, 1)    AS `Recall (%)`,
    ROUND(map50 * 100, 1)           AS `mAP@50 (%)`,
    ROUND(map50_95 * 100, 1)        AS `mAP@50-95 (%)`,
    status
FROM gold.ml_experiment_log
ORDER BY map50_95 DESC;
```

### Bước 7: Tạo SQL Dashboard

- [ ] Vào menu trái → **Dashboards** → **Create Dashboard**
- [ ] Đặt tên: `VisionAI - Monitoring Dashboard`
- [ ] Thêm các Widget (bấm **Add** → **Visualization**):

**Widget 1: Số lượng ảnh theo Camera (Bar Chart)**
```sql
SELECT camera_id, total_captures
FROM gold.detection_by_camera
ORDER BY total_captures DESC;
```
→ Chọn kiểu biểu đồ: **Bar Chart** | X: `camera_id` | Y: `total_captures`

**Widget 2: Phân bố theo Ca làm việc (Pie Chart)**
```sql
SELECT time_shift, total_captures
FROM gold.detection_by_shift;
```
→ Chọn kiểu biểu đồ: **Pie Chart** | Label: `time_shift` | Value: `total_captures`

**Widget 3: Hoạt động theo Giờ (Line Chart)**
```sql
SELECT hour_of_day, total_captures, active_cameras
FROM gold.hourly_activity
ORDER BY hour_of_day;
```
→ Chọn kiểu biểu đồ: **Line Chart** | X: `hour_of_day` | Y: `total_captures`

**Widget 4: Chất lượng ảnh theo Camera (Stacked Bar)**
```sql
SELECT camera_id, high_quality_count, medium_quality_count, low_quality_count
FROM gold.detection_by_camera;
```
→ Chọn kiểu biểu đồ: **Stacked Bar** | X: `camera_id`

**Widget 5: Bảng MLflow - Model Performance**
```sql
SELECT
    model_name, model_version,
    ROUND(precision_score * 100, 1) AS `Precision%`,
    ROUND(map50 * 100, 1) AS `mAP50%`,
    ROUND(map50_95 * 100, 1) AS `mAP50-95%`,
    training_time, status
FROM gold.ml_experiment_log
ORDER BY map50_95 DESC;
```
→ Chọn kiểu hiển thị: **Table**

---

## 📅 NGÀY 5 — Thứ Năm 24/4 🚫 ĐỪNG làm tính năng mới

**🎯 Mục tiêu: Chuẩn bị kịch bản demo hoàn chỉnh**

- [ ] Mở lại từng bảng Bronze → Silver → Gold, chạy `SELECT *` để chắc chắn data còn nguyên.
- [ ] Mở Dashboard, bấm **Refresh** để biểu đồ cập nhật.
- [ ] Chuẩn bị **script demo** theo thứ tự:

```text
1. Mở GitHub repo             → Show cấu trúc thư mục chuyên nghiệp
2. Mở GitHub Actions          → Show CI test tự động chạy xanh lá
3. Mở Databricks SQL Editor   → Show lệnh SQL tạo Bronze table (giải thích Data Ingestion)
4. Mở bảng Bronze             → Show raw data gốc từ camera
5. Mở bảng Silver             → Show data đã qua xử lý (Feature Engineering)
6. Mở bảng Gold               → Show data đã tổng hợp (Business Aggregation)
7. Mở bảng ML Experiment Log  → Show model tracking (thay thế MLflow UI)
8. Mở SQL Dashboard           → Show 5 biểu đồ trực quan đẹp
9. Mở Architecture Slide      → Tổng kết kiến trúc Medallion + CI/CD
```

- [ ] **Chụp screenshot toàn bộ 9 bước** (phòng trường hợp mất mạng khi demo)

---

## 📅 NGÀY 6 — Thứ 7, 25/04/2026 🎤 BÁO CÁO

```text
✅ Đăng nhập Databricks trước 15 phút để chắc chắn SQL Warehouse đang Running
✅ Demo đúng theo 9 bước kịch bản đã chuẩn bị
✅ Nếu giảng viên hỏi "sao không dùng Cluster?":
   → Trả lời: "Dự án sử dụng kiến trúc Serverless để tối ưu chi phí.
      Serverless SQL tự động scale, không cần quản lý hạ tầng,
      phù hợp với mô hình Data Lakehouse hiện đại."
❌ Tuyệt đối không sửa code hay làm thêm tính năng vào sáng hôm đó
```

---

## 🎯 Kết Quả Demo Đạt Được

| Tiêu chí | Demo Live? | Ghi chú |
|---|:---:|---|
| GitHub Repo chuẩn kỹ sư | ✅ | Cấu trúc thư mục rõ ràng |
| GitHub Actions CI | ✅ | Automated Testing |
| Databricks SQL Warehouse | ✅ | Serverless, tự động scale |
| Bronze Delta Table | ✅ | Raw camera data |
| Silver Delta Table | ✅ | Feature Engineering |
| Gold Delta Tables (x3) | ✅ | Business Aggregation |
| ML Experiment Tracking | ✅ | Bảng SQL thay MLflow UI |
| SQL Dashboard (5 biểu đồ) | ✅ | Trực quan hóa KPI |

---

## 🗣️ Câu trả lời khi giảng viên hỏi

| Câu hỏi | Trả lời |
|---|---|
| Sao không dùng Python/PySpark? | "Kiến trúc này ưu tiên SQL-first approach theo xu hướng Data Lakehouse hiện đại. SQL dễ bảo trì và nhiều người trong team có thể đọc hiểu được." |
| MLflow thật ở đâu? | "Trong production sẽ dùng MLflow Python SDK. Ở đây em mô phỏng bằng bảng SQL để demo luồng tracking. Kiến trúc đã thiết kế sẵn để tích hợp MLflow khi có All-purpose Compute." |
| Serverless có đắt không? | "Serverless SQL chỉ tính tiền khi có query chạy, không có idle cost. Phù hợp cho workload không liên tục như camera AI chụp theo lịch." |
| Data thật ở đâu? | "Data mẫu được generate để demo pipeline. Trong production, camera sẽ gửi metadata qua API, được Auto Loader ingest vào Bronze tự động." |

---

*Cập nhật ngày 23/04/2026 | VisionAI Team | Databricks Serverless SQL + GitHub CI/CD*
