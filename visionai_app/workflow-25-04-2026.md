# 📋 VisionAI — Workflow Kế Hoạch Demo
## Azure Databricks + GitHub CI/CD | Deadline: Thứ 7, 25/04/2026

> **Nguyên tắc:** Chỉ làm những thứ có thể **DEMO LIVE** khi báo cáo.  
> Bỏ qua những gì chỉ là "nice to have".

---

## 📅 NGÀY 1 — Tối Chủ Nhật 19/4 (~2-3 tiếng)

**🎯 Mục tiêu: Dựng GitHub repo đúng chuẩn**

- [ ] Tạo GitHub repo: `visionai-databricks`
- [ ] Tạo cấu trúc thư mục:

```
visionai-databricks/
├── databricks.yml
├── src/
│   ├── ingestion/
│   │   └── camera_ingest.py
│   ├── processing/
│   │   └── image_processing.py
│   └── training/
│       └── yolov8_training.py
├── tests/
│   └── test_ingestion.py
├── .github/
│   └── workflows/
│       └── ci.yml
└── requirements.txt
```

- [ ] Push lên GitHub
- [ ] Verify repo nhìn professional

**✅ Kết quả:** Repo sạch, có cấu trúc đúng kiến trúc.

---

## 📅 NGÀY 2 — Thứ Hai 20/4 ⭐ (Ưu tiên cao nhất)

**🎯 Mục tiêu: GitHub Actions CI chạy được**

- [ ] Viết `ci.yml` trigger khi push/PR:
  - Bước 1: `pip install` + `pytest`
  - Bước 2: `databricks bundle validate`
- [ ] Setup secrets trên GitHub:
  - `Settings → Secrets → DATABRICKS_HOST`
  - `Settings → Secrets → DATABRICKS_TOKEN`
- [ ] Push 1 commit → Xem GitHub Actions chạy **xanh lá** ✅
- [ ] Chụp screenshot GitHub Actions passed

> 💡 **Lý do ưu tiên:** Đây là phần "CI/CD" của diagram —  
> nếu demo được Actions chạy live thì bài báo cáo sẽ rất thuyết phục.

---

## 📅 NGÀY 3 — Thứ Ba 21/4

**🎯 Mục tiêu: Databricks Workspace + Medallion Bronze/Silver**

- [ ] Tạo Databricks workspace (Azure)
- [ ] Tạo Unity Catalog: `visionai_catalog`
- [ ] Upload 10-20 ảnh mẫu lên Azure Blob Storage (camera feeds)
- [ ] Viết & chạy **Notebook Bronze** (`camera_ingest.py`):
  - Đọc JSON từ Blob bằng Auto Loader
  - Ghi vào Bronze Delta table
- [ ] Viết & chạy **Notebook Silver** (`image_processing.py`):
  - Đọc Bronze
  - Thêm cột: `hour_of_day`, `quality_bucket`, `location_type`
  - Ghi vào Silver Delta table
- [ ] Verify Bronze/Silver có data

---

## 📅 NGÀY 4 — Thứ Tư 22/4

**🎯 Mục tiêu: MLflow Training + Gold Table**

- [ ] Viết & chạy **Notebook Training** (`yolov8_training.py`):
  - `mlflow.set_experiment("/VisionAI/yolov8")`
  - Train hoặc **simulate metrics** (nếu không có GPU)
  - `log_params` + `log_metrics` + `log_model`
  - Register model vào **Model Registry**
- [ ] Viết & chạy **Notebook Gold**:
  - Đọc Silver
  - Aggregate: `COUNT detections`, `AVG confidence` by camera
  - Ghi vào Gold Delta table
- [ ] Verify MLflow UI → thấy experiment + runs
- [ ] Verify Model Registry → thấy model trạng thái `Staging`

> 💡 **Tip:** Không cần train YOLOv8 thật. Simulate metrics bằng `numpy random`  
> là đủ để demo MLflow tracking.

---

## 📅 NGÀY 5 — Thứ Năm 23/4

**🎯 Mục tiêu: Databricks Workflows + SQL Dashboard**

- [ ] Tạo **Databricks Workflow** (Master Scheduler DAG):
  - Task 1: chạy Bronze notebook
  - Task 2: chạy Silver notebook *(depends_on: Task 1)*
  - Task 3: chạy Gold notebook *(depends_on: Task 2)*
- [ ] Chạy workflow 1 lần → Verify 3 tasks **xanh lá** ✅
- [ ] Tạo **SQL Dashboard** (Observability):

```sql
-- Query 1: Detection by Camera
SELECT camera_id, COUNT(*) AS total_detections
FROM visionai_catalog.gold.detection_results
GROUP BY camera_id;

-- Query 2: Average Confidence
SELECT DATE(detection_ts), AVG(avg_confidence)
FROM visionai_catalog.gold.detection_results
GROUP BY 1 ORDER BY 1;
```

- [ ] Chụp/record toàn bộ flow để có **backup** khi demo

---

## 📅 NGÀY 6 — Thứ Sáu 24/4 🚫 ĐỪNG làm tính năng mới

**🎯 Mục tiêu: Chuẩn bị demo hoàn chỉnh**

- [ ] End-to-end test: `Push code → GitHub Actions → Databricks pipeline`
- [ ] Chuẩn bị **script demo** theo thứ tự:

```
1. Mở GitHub repo        → show cấu trúc thư mục
2. Mở GitHub Actions     → show CI chạy xanh
3. Mở Databricks         → show Bronze / Silver / Gold tables
4. Mở MLflow UI          → show experiment + model registry
5. Mở Workflows          → show DAG đã chạy thành công
6. Mở SQL Dashboard      → show KPIs & charts
7. Mở Architecture Diagram (hình V4)
```

- [ ] **Chụp screenshot backup TẤT CẢ** *(phòng trường hợp mất mạng khi demo live)*

---

## 📅 NGÀY 7 — Thứ 7, 25/04/2026 🎤 BÁO CÁO

```
✅ Demo theo script đã chuẩn bị sẵn
❌ Không làm thêm bất kỳ thứ gì vào sáng hôm đó
```

---

## 🎯 Kết Quả Demo Cuối Cùng

| Thành phần | Có thể Demo Live? |
|---|:---:|
| GitHub repo (cấu trúc chuẩn) | ✅ |
| GitHub Actions CI (test + validate) | ✅ |
| Azure Databricks Workspace | ✅ |
| Bronze / Silver / Gold Delta Tables | ✅ |
| MLflow Experiments + Model Registry | ✅ |
| Databricks Workflows DAG | ✅ |
| SQL Dashboard (Observability) | ✅ |
| Architecture Diagram V4 | ✅ (Slide) |

---

## ❌ Bỏ Qua — Không Cần Cho Demo

| Thành phần | Lý do bỏ |
|---|---|
| Model Serving Endpoint | Phức tạp, không đủ thời gian |
| Full OIDC Production Setup | Dùng PAT token demo là đủ |
| Staging vs Production Workspace | 1 workspace demo là đủ |
| Azure Key Vault Integration | Đề cập trong slide/diagram là đủ |

---

*Tạo ngày 20/04/2026 | VisionAI Team | Azure Databricks + GitHub CI/CD*
